import 'package:flutter/material.dart';

class AddServiceRecordScreen extends StatelessWidget {
  const AddServiceRecordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Service Record'),
      ),
      body: const Center(
        child: Text('Placeholder for Add Service Record Screen'),
      ),
    );
  }
}
